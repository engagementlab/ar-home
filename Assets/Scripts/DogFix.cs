using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DogFix : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.Rotate(0, -180, 0);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
